# Core ML & extraction
PyPDF2>=3.0.0
python-docx>=1.1.0
scikit-learn>=1.4.0

# Level 2 — Better AI model
sentence-transformers>=2.7.0

# Level 2 — Language detection
langdetect>=1.0.9

# File type support
openpyxl>=3.1.0
python-pptx>=0.6.21
Pillow>=10.0.0
extract-msg>=0.28.0

# Watch Mode
watchdog>=4.0.0

# Progress bar
tqdm>=4.66.0

# Level 3 — Web app & API
flask>=3.0.0

# Level 3 — Desktop installer (run python build.py to create .exe)
# pyinstaller>=6.0.0

# Optional: OCR for images (requires Tesseract installed separately)
# pytesseract>=0.3.10

# Testing
pytest>=8.0.0

# Level 3 — Statistics Dashboard
matplotlib>=3.8.0

# AI Smart Rename (uses NVIDIA free API)
openai>=1.0.0
